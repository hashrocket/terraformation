require 'spec_helper'

describe <%= class_name %> do
  subject do
<% if factory? -%>
    Factory.build(<%= full_name.to_sym.inspect %>)
<% elsif exemplar? -%>
    <%= class_name %>.spawn
<% else -%>
    <%= class_name %>.new
<% end -%>
  end

  it "should be valid" do
    should be_valid
  end
end
